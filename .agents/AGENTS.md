# Project Behavioral Rules

- **Git Commits & Pushes**: Do NOT execute `git commit` or `git push` automatically. Prepare changes in the working directory and present them to the user, allowing the user to commit/push or explicitly request a commit/push.
